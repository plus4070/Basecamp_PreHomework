<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace= "userControlMapper" >
    <select id ="selectSample" parameterType="java.util.HashMap" resultType= "java.util.HashMap">
        SELECT *
        FROM BASECAMP_PREHOMEWORK
    </select>

  	<insert id ="insertBoard" parameterType="java.util.HashMap" >
        <![CDATA[
			INSERT INTO BASECAMP_PREHOMEWORK
			(
				BID,
			    BDATE, 
			    EMAIL, 
			    PASSWORD, 
			    BCONTENT,
			    BEDITDATE, 
			)
			VALUES
			(
				SEQ_TB_BOARD_IDX.NEXTVAL, 
			    SYSDATE, 
			    #{EMAIL}, 
			    #{PASSWORD}, 
			    #{CONTENT}, 
			    0
			)
		]]>
    </insert>

</mapper>